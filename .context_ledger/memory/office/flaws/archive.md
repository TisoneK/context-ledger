# Flaws Log — archive (verbatim moves from log.md)

Entries below were explicitly marked `RESOLVED` / fixed in the live log
and moved here verbatim (no edits), per the log's pruning rule. Startup
reads only the live log; this archive stays in git, grep-able.

## 2026-09-11 — Kai / glm-5.3-flash (Session 2)

- **Flaw:** `ledger-gates.ps1`'s gate verdict can clear a red gate — reported by a fleet project (Asha, S467, flaw reported upstream 2026-09-11) and confirmed here by reading `core/bin/ledger-gates.ps1` `Run-One` (lines 65–82).
- **Symptom:** a gated command whose failing tool is piped into a succeeding stdout consumer (e.g. `ci-check.sh 2>&1 | tee out.txt`) reports FAILED text but exits 0: `$LASTEXITCODE` after the pipeline is the *last* native command's (tee = 0), and `$?` follows it, so the gate passes. Silent failures still fail correctly, which is why some gate failures bite and others don't.
- **Root cause:** `Run-One` trusts `$LASTEXITCODE`/`$?` from the whole scriptblock; PowerShell only surfaces the pipeline tail's status, not every command's. Pre-existing — present before core 1.0.0 (not a regression from the office work; the 1.0.0 gate change was the checkpoint notice only).
- **Suggested fix:** run the gated text via an explicit subshell/`cmd /c` wrapper that propagates the real exit code, or fail if `$LASTEXITCODE` is non-null-and-nonzero OR `$?` is false AND reject compound commands whose last pipeline stage is not the tool under test; add a package test: a gated `failing-cmd | tee` must fail the gate. Until fixed, projects should treat gate stdout as the verdict, never rc alone.
- **FIXED (2026-09-11, Noor / S003):** shipped in core 1.0.1 (commits 8e5a8a6 fix, c97418d release) with the package test the entry asked for (`tests/run-tests.sh`, 11/11 green across both editions). sh edition: gated commands run under `set -o pipefail` where the shell (or a bash on PATH) supports it; on a shell without pipefail a gated top-level pipeline is rejected with a fix-it message instead of silently passing. PowerShell edition: gated text is parser-audited first — a pipeline with two or more external stages (or an unresolvable one) is rejected; at most one external stage runs, and the verdict fails on the exit code OR `$?` — either signal. The "treat gate stdout as the verdict" workaround is retired; the rc is the verdict again.

## 2026-09-11 — Kai / glm-5.3-flash (Session 2, addendum)

- **Flaw:** `ledger-sync.ps1`'s office migration (`Migrate-OfficeLayout`, shipped in core 1.0.0 — my code, named per the repo rule) corrupts non-ASCII characters when it rewrites `history.conf`. Confirmed by reproduction: a UTF-8 `history.conf` whose comment holds an em-dash (`e2 80 94`) comes back with cp1252 mojibake (`â€"`) after `ledger-sync.ps1 migrate` on Windows PowerShell 5.1.
- **Symptom:** comments in the rewritten config are mojibake; the file stays *functional* (`office_size=20` parses fine, tools read only the keys), so it fails silently — grep-ability and byte-cleanliness of the config are lost, and any future non-ASCII content in the file gets mangled the same way.
- **Root cause:** `Get-Content -LiteralPath $hc` without `-Encoding UTF8` on Windows PowerShell 5.1 reads a BOM-less UTF-8 file in the system ANSI codepage (cp1252); the following `[IO.File]::WriteAllText(..., UTF8-no-BOM)` then re-encodes the already-mangled text as clean UTF-8 — corruption baked in. The 1.0.0 verification matrix missed it because the scratch `history.conf` used ASCII-only test data; the shipped template comment (`— read by ledger-history.`) is exactly the content that triggers it.
- **Suggested fix:** one line — read with `-Encoding UTF8` (or `[IO.File]::ReadAllText` with an explicit UTF-8 encoding) before the rewrite. Bundle into the in-flight **core 1.0.1 patch** (same release as the gate-verdict fix, owned by the other agent — do not duplicate). Class-level prevention for the same release: audit every `Get-Content`/`Set-Content` in the ps1 ports for missing explicit UTF-8 encoding — PS 5.1's ANSI defaults make every one of them a latent corruption site.
- **Status:** open — assigned to the 1.0.1 patch (another agent is working on it).
- **Correction (same session, before any other session reads this):** the version assignment above is wrong — this flaw is assigned to **core 1.0.2**, not 1.0.1. The other agent's in-flight 1.0.1 covers the gate-verdict fix only; the cp1252 encoding fix + the ps1 UTF-8 encoding audit ship as 1.0.2.
- **FIXED (2026-09-11, Kai / S002):** shipped in core 1.0.2. The migration read is `-Encoding UTF8` explicit; the full audit landed with it — all 27 `Get-Content` sites across the six ps1 ports are UTF-8 explicit, `Write-Lock` emits the sh port's exact em-dash byte sequence (from `[char]0x2014` — ps1 string literals must stay pure ASCII, because PS 5.1 *parses* BOM-less ps1 source as cp1252 and a literal double-encodes on write), and the rename sweep reads/writes UTF-8. tests/run-tests.sh carries three regressions (em-dash survives the migration; key rename intact; core.lock byte-identical to sh, no BOM); 14/14 green on Git Bash + Windows PowerShell 5.1.

## 2026-09-14 — Zuri / glm-5.3-flash (Session 8)

- **Flaw:** `ledger-mem prune`'s archive-eligibility heuristic over-matches. Its closed-marker regex (`superseded|RESOLVED|fixed in package|…`) scans every line of a segment, so it flags (a) the leading `## ADR-N`/log **template comment** (whose placeholder literally reads "superseded by ADR-M") and (b) any real entry whose body merely *mentions* the word — e.g. ADR-5, written to describe the compaction rule, contains "resolved/superseded entries move verbatim…". On the fresh core 1.1.0 `plans/decisions.md`, both live ADRs were reported "archive-eligible" though every one is `accepted`.
- **Symptom:** `prune` reported 2 of 6 decisions archive-eligible; `--list` named the template comment + ADR-5. Acting on it would have wrongly archived a current ADR.
- **Root cause:** keyword match against arbitrary body text + no exclusion of the seeded template comment block; the "closed" signal should be the entry's own `**Status:**` line, not any occurrence of the word.
- **Suggested fix:** package (both `ledger-mem` + `.ps1`): scope the closed-marker to the entry's `**Status:**` line only (or `^-\s*\*\*Status:\*\*.*(superseded|resolved)`), and skip the `<!-- … -->` template preamble when segmenting so the `ADR-N` placeholder is never a candidate.
- **Status:** open — advisory-only, so no data was at risk; logged as `Upstream: candidate` for a future PATCH to the prune heuristic.
- **Fixed in package:** core 1.1.2 (fix + release 36720b9, self-hosted 0714a2f, Session 9) — the closed-marker is scoped to each entry's own `**Status:**` line, template comments are never segmented, and the schema states the scoping. Verified live: `prune --list` on this office now named exactly the two genuinely-fixed entries (Milo Session 4, Ines Session 7) and none of the accepted ADRs.

## 2026-09-11 — Kai / glm-5.3-flash (Session 2, second addendum)

- **Flaw:** my own staging mistake — my 1.0.0-closeout commit (01e76ac) used `git add .context_ledger/memory/` and swept an uncommitted leftover into the commit: Noor's (S003) roster row, which her clock-out (1b894f7) had already removed correctly. The board then showed a peer in the office who had left.
- **Symptom:** the committed roster claimed Noor was present after her session ended; caught this session when checking the board before re-checking in, and traced via the roster's commit history (her clock-out removed the row; no later commit touched the file — the row lived on uncommitted in her working tree until my directory-wide add swept it).
- **Root cause:** directory-wide `git add` on the memory zone, where peer sessions leave uncommitted state. The 0.20.0 additive-edit lesson (your diff shows exactly your row) applies to staging too.
- **Suggested fix:** habit + package: stage named files after reviewing `git diff` (never `git add <dir>` under `.context_ledger/memory/`); `ledger-mem check` could warn when a committed roster row belongs to a session whose clock-out commit already exists.
- **Status:** fixed here — her stale row removed in this session's check-in commit (verified: her clock-out commit + Session 3 entry prove she left); logging rule honored.

## 2026-09-11 — Milo / glm-5.3-flash (Session 4)

- **Flaw:** the 1.0.1 gate-verdict fix was incomplete. It closed the piped-consumer case (`failing-cmd | tee`), but `Run-One` still ran the gated command inline, so a configured command that PRINTS to stdout and exits nonzero returned `@(lines..., $false)` — `-not` on a non-empty array never registered the failure, and the gate printed `FAILED (N)` and then `GATE PASSED` with rc=0. The archived 1.0.1 entry's "the rc is the verdict again" was therefore premature; the interim "read gate stdout, never trust rc on Windows" rule stayed necessary through 1.0.2.
- **Symptom:** fleet projects re-verified the flaw open on 1.0.0 (Asha, S467) and on 1.0.2 (Amir, S468, three evidence legs including a real pre-commit `FAILED (1)` → `GATE PASSED`). The 1.0.1 package regression never caught it because it exercised `cmd /c exit 3` — a silent failure, exactly the case that already worked. The maintainer repo picked the flaw up from the fleet-sync evidence report and confirmed it by structural read + a minimal probe before patching.
- **Root cause:** PowerShell functions return everything on their output pipeline. `Invoke-ChildScript` already used the correct capture-and-host-stream pattern; `Run-One` never got it — the file's own comments documented the trap for `Log` while missing the child-command path.
- **Suggested fix (applied in core 1.0.3):** capture the child's stdout (`$out = & ([scriptblock]::Create($Text))`), judge the verdict BEFORE re-emitting — the `Write-Host` loop resets `$?`, so the naive capture patch would swap one mask for another — then re-emit on the host stream. Same-class hardening: `Invoke-ChildScript`'s `$?` fallback moved before its re-emit loop. Regression in tests/run-tests.sh: a chatty failing command must fail the gate with output visible and the `FAILED (N)` verdict line asserted, never the wrapper rc alone (suite 14 → 22, both editions). The sh port is unaffected (real child shell, exit propagates, stdout never passes through a function return).
- **Status:** fixed in package core 1.0.3 (fix 6b710d4, release 2609f75, self-hosted 5ad5955). The interim rc rule retires per project once its vendored core reaches 1.0.3.

## 2026-09-12 — Ines / glm-5.3-flash (Session 7)

- **Flaw:** the protocol sequenced the roster check-in after the startup read — Step 3's read order ran the memory files before the sign-in bullet, and the ENTRY rule ("do not edit any file until Phase 1 is complete") read as backing for deferring it. A worker launched by the supervisor spends the long read on protocol and product code first, so the board stays empty while they think.
- **Symptom:** two workers launched together both see an empty office, each concludes they are alone, and each takes the same codename/session number they thought of at the door; when they finally notice each other they are strangers mid-analysis arguing over who takes the main tree. The supervisor reported the exact scenario ("workers should check in at the entrance not after working").
- **Root cause:** presence was sequenced as a pre-work formality rather than the session's first write — the push, the only thing peers can actually see, came after everything interesting.
- **Suggested fix (applied in core 1.0.6):** the check-in is the session's FIRST write — signing needs exactly two files (roster + last session entry), so read those, push, then do the deep read; ENTRY names the check-in row the one sanctioned edit inside Phase 1. The push claims the codename (whoever's check-in commit is already on origin keeps it; on a collision the earlier commit wins and the later worker renumbers their own row only — never drops a peer's row). The main tree is settled at the door by conversation: work already in flight keeps it, the other worker isolates on a branch/worktree, both declare the shared session/issue. Shipped to both editions, the kickoff Step 2 (retitled "Sign in at the door"), the AGENTS digest rule 5, the roster template preamble, the vendored README, and the schema (roster spec, reading order, office lifecycle).
- **Status:** fixed in package core 1.0.6 (release bd39226, self-hosted 21ecc1c).

## 2026-09-18 — Leo / claude-sonnet-5 (Session 12)

- **Flaw:** `tests/run-tests.sh` crashed with `PS_SCRATCH: unbound variable` on its own final cleanup line (`rm -rf "$SH_SCRATCH" "$PS_SCRATCH"`) on any machine with no `pwsh`/`powershell` on `PATH` — this Mac included, which is presumably every machine that has ever run this suite here.
- **Symptom:** the whole suite (23 real tests, all passing) reported no pass/fail tally at all — it just died under `set -u` after the last test, `|| true` on that line notwithstanding (a `set -u` unbound-variable abort happens during word expansion, before the command or its `||` ever runs).
- **Root cause:** `PS_SCRATCH` is assigned only inside the PowerShell-gated test block, itself skipped when no PS engine is found — so the unconditional cleanup at the bottom of the file references a variable that, on this machine, was never set.
- **Suggested fix:** applied directly, safe one-liner: `"${PS_SCRATCH:-}"` instead of `"$PS_SCRATCH"`. Filed as a flaw rather than silently fixed because it means the suite's exit status/tally has likely never been trustworthy on a Mac without `pwsh` installed — worth checking whether CI (if any) runs where `pwsh` is present, which would explain why this was never caught.
- **Status:** fixed in package (this commit) — `tests/run-tests.sh` line ~661.

## 2026-09-18 — Leo / claude-sonnet-5 (Session 12, addendum)

- **Flaw:** found via a live bootstrap test (fresh scratch project, core 2.0.0) rather than the unit-test fixtures — two bugs, both in code shipped this session. (1) `ledger-state generate`'s `Get-Field`/`field()` helper read a value from inside a file's own `<!-- -->` template comment when no real, uncommented line for that key existed yet (a freshly bootstrapped `workflows/active.md` has no "Target" line at all outside the comment) — `STATE.md` printed the comment's placeholder text (`<general sweep | refactor <path> | ...>`) as if it were the project's actual standing Target. (2) `workflows/active.md`'s template comment itself is invalid: it nests a second `<!-- ... -->` inside the outer `<!-- TEMPLATE ... -->` block (an aside on the Protocol field), which is not valid HTML/Markdown comment syntax — the first `-->` closes the *outer* comment early, so every field listed after it (Protocol location, Since, Target, …) was, technically, outside any comment at all. Bug (1)'s naive one-level `<!-- -->` toggle exposed this: it treated the nested block's `-->` as closing everything, un-masking the "live" copies of those fields long enough for `field()` to read one that had no real counterpart.
- **Symptom:** `STATE.md`'s "Standing params" section showed a raw template placeholder as a real value on a fresh bootstrap.
- **Root cause:** two independent issues compounding — `field()` had no comment-skipping at all (unlike `log_digest`/`check_roster`/`prune`, which already skip single-level `<!-- -->` blocks correctly); and the `workflows/active.md` template nests comments, which no single-level skip logic (mine or a hypothetical fixed one) can honor, because the nesting itself isn't valid — the file's real structure doesn't match what the markup visually suggests.
- **Suggested fix:** applied directly. `field()` (both ports) now skips `<!-- -->` blocks (single-level, matching every other parser in this codebase). `workflows/active.md`'s template: the nested aside is now a plain parenthetical instead of a second HTML comment (no meaning lost, valid structure restored); the real skeleton below the comment gained a `- **Target:** —` line it was simply missing (documented in the comment, never seeded). General lesson for future template edits: `<!-- -->` never nests in this codebase's parsers — an aside inside a comment must stay non-comment text.
- **Status:** fixed in package (core 2.0.1) — `ledger-state`/`ledger-state.ps1`, `core/templates/memory/workflows/active.md`.

## 2026-09-19 — Priya / claude-sonnet-5 (Session 14)

- **Flaw:** core 2.0.0's token/context optimization pass (Session 12) reduced `AGENTS.md` from a 158-line digest to a ~28-line pure router ("go read kickoff.md and follow it") without checking that reduction against this repo's own prior design requirement — recorded in the maintainer's own memory, not re-derived at the time — that `AGENTS.md` is the deliberate **weak-agent floor**: some sessions on this repo run on smaller/weaker models that read only the root digest and never reliably work through a multi-file, multi-phase routing chain. The supervisor reported the real-world consequence directly: agents were fighting during initialization because check-in stopped happening before analysis — sessions jumped straight to "the deep read," didn't see a live peer, and by the time they got around to checking in it was too late.
- **Symptom:** two (or more) sessions starting close together no longer reliably saw each other on the roster before beginning work, reintroducing the exact collision Session 7 (Ines, core 1.0.6) had already fixed once: "two workers launched together both see an empty office, both take the same codename." The fix regressed silently — no test caught it, because the unit suite and even the live-bootstrap test from Session 12 both exercised a single agent following the documented phases faithfully, never a weak/lazy reader that stops at `AGENTS.md` or reads a whole file before acting on any of it.
- **Root cause:** de-duplication removed *all* restated content from `AGENTS.md`, including the one line of restated content that was load-bearing on purpose — check-in-first was previously stated redundantly in AGENTS.md (as one of "if you read nothing else, obey these rules"), kickoff.md, and the edition's Ten Binding Rules specifically *because* it is cheap to state and catastrophically expensive to miss, and because different agents stop reading at different depths. Token-cost optimization treated all repetition as waste; some repetition was a deliberate safety margin against exactly this failure mode, not an oversight.
- **Suggested fix:** applied directly (core 2.0.2): restored a short, standalone "check in before reading anything else — including the rest of this file" directive to both `AGENTS.md` and `CLAUDE.md` (previously CLAUDE.md never had this restated either, even pre-2.0.0 — it always deferred to AGENTS.md/kickoff.md, which no longer holds since AGENTS.md's own restatement was removed), placed *before* the "go read kickoff.md" pointer rather than after. Also strengthened `kickoff.md`'s own Phases intro: explicit instruction to execute each phase before reading the next, with a named recovery step ("if you already skimmed Phase 3+, stop and push Phase 2 now"). General lesson for future protocol edits: before removing a restated rule as "duplication," check whether the duplication was serving as a safety margin for a documented failure mode (search `flaws/log.md` and `plans/decisions.md` for the rule's origin) — token cost and redundancy-as-safety-margin are a real trade-off, not a free win in one direction.
- **Status:** fixed in package (core 2.0.2) — `core/templates/AGENTS.md`, `core/templates/CLAUDE.md`, `.context_ledger/kickoff.md`, `ledger-schema.md`'s Translation layer section corrected to match.


## 2026-09-19 — Omar / claude-sonnet-5 (Session 15)

- **Flaw:** the supervisor felt core 2.0.0's "token optimization" MAJOR release had downgraded rather than upgraded the protocol, beyond the one check-in regression 2.0.2 already fixed. Diffing 2.0.0 through 2.0.2 directly against the last v1 release (1.2.0) — rather than trusting each release's own changelog account of itself — confirmed four distinct problems from the same rewrite: (1) `kickoff.md`'s Phase 4 routing table pointed at `core/rules/playbooks/pitfalls.md`, a file that has never existed (Common Pitfalls stayed inline by design; the table was never corrected to match) — the same phantom file was also listed in `ledger-schema.md`'s directory diagram; (2) two v1 rules — "`.context_ledger/` reflects what was true when written; if it contradicts the codebase, the codebase wins" and "small and current beats big and stale (~10-line session entries)" — were deleted outright with zero replacement anywhere in the vendored core (confirmed by grep across the whole tree, not just the obvious spots); (3) `AGENTS.md`/`CLAUDE.md` (the "weak-agent floor" — the one file some sessions ever read in full) only got the one restated rule (check-in) that had already caused a reported incident; ~8 other rules 2.0.0 cut from the same files were left exposed to the identical failure mode with no incident yet on record; (4) the playbook extraction itself preserved content faithfully, but silently changed a guarantee — v1 read the Code Review Checklist unconditionally every session, while 2.0.0's routing table made it conditional on the agent correctly self-classifying its own task, defaulting to "(nothing more)" for an ordinary product-code change.
- **Symptom:** (1) a session following the table literally hits a dead reference and either gives up on Common Pitfalls or gets confused; three releases (2.0.0, 2.0.1, 2.0.2), including two live-tested bug-fix releases, shipped without catching it. (2) two real epistemic/hygiene safeguards silently absent — not hypothetical: this office's own `sessions.md` entries for Sessions 11–14 are dense multi-paragraph blocks, not ~10-line summaries, exactly the bloat rule #12 existed to prevent. (3) same exposure class as the 2.0.2 collision, just not yet triggered — e.g. nothing stops a session that only reads the floor file from blowing past the `office_size` door trigger. (4) a small bug fix that happens to touch security-sensitive code gets no security-checklist prompt unless the agent notices that itself.
- **Root cause:** 2.0.0's de-duplication pass treated all restated content as pure waste and didn't distinguish "said three times because nobody proofread" from "said three times as a deliberate safety margin for agents that don't reliably chain through a multi-file routing sequence" — the same root cause 2.0.2's entry already named for check-in specifically, here shown to apply to the rest of the cut list too, plus two outright content deletions and one broken cross-reference that were never about redundancy at all.
- **Suggested fix:** package (done this session, core 2.0.3): restored `office_size` close-trigger, no-secrets, two-surface-commit-split, and not-done-until-pushed to `AGENTS.md`/`CLAUDE.md` (same treatment 2.0.2 gave check-in); restored "verify before trusting" and "small and current" as items 11–12 of the renamed "Binding Rules" (was "Ten Binding Rules") in both editions; fixed the `pitfalls.md` reference in `kickoff.md`'s Phase 4 table and `ledger-schema.md`'s directory diagram to point at the edition's inline Common Pitfalls section instead; changed the Phase 4 table's and both editions' Playbooks index's default row so `code-review.md` loads for any product-code task, not only "a new feature or substantial review." General lesson for future de-duplication passes: before cutting a restated rule, check whether the repetition was a documented safety margin — and diff against the actual prior release, not just the new release's own account of what it changed.
- **Status:** fixed in package 2.0.3 (self-hosted this session).


## 2026-09-23 — Ilya / deepseek/deepseek-flash (Session 16)

- **Flaw:** the package can ship PowerShell ports that no engine can parse on a stock Windows box, and nothing in the release path notices when the releasing host has no PowerShell. Reported upstream by a consumer project that migrated to 2.0.3 and could not run a single gate.
- **Symptom:** on core 2.0.3, `ledger-sync verify` — and with it `pre-commit`, `integration` and `exit` — returned 3 on Windows. `ledger-state.ps1` did not parse on *any* engine (`"**$Label:"` reads as a scope-qualified variable reference), and two ports carried non-ASCII bytes that Windows PowerShell 5.1 decodes into string terminators. The authoring host was macOS with no PowerShell, and `tests/run-tests.sh` skips its ps1 half there by design, so the suite stayed green while the shipped ports were dead on Windows.
- **Root cause:** verification of the ps1 half was tied to the engine on the host — skip with a notice where none exists, check through only the *first* engine found where one does (that was `powershell` 5.1, so a pwsh-only-parseable port passed and then failed under the `.cmd` launchers, which target 5.1 on purpose). Both halves assume the host represents the machines the release lands on; for a Windows-only port family it does not. Sharper still: the rule this release violated was *already written down* — `system/ai-models.md` carries Kai's 2026-09-11 observation "keep ps1 string literals pure ASCII and emit non-ASCII output via `[char]0x2014`-style code points" — but an observation is not a check, and nothing in the release path read or enforced it.
- **Suggested fix:** package (done in 2.0.4): an engine-free encoding guard in both editions — a ps1 carrying a non-ASCII byte fails verify on any host, macOS included — a parse check under *every* engine on PATH, and four suite assertions including a pure-ASCII invariant for `core/bin/*.ps1`. Process rule worth adding to README's maintainer discipline: a release touching a ps1 port is verified on a host that has a PowerShell engine, or the guard is deliberately relied on as the portable net.
- **Status:** fixed in package 2.0.4 (efb9665) — ports parse under 5.1 and pwsh 7; guard, every-engine check and tests in place.

---
